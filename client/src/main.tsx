import React, { useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { io, Socket } from 'socket.io-client';
import QRCode from 'qrcode';
import { ArrowRight, Check, Copy, MonitorPlay, Radio, Users, Zap } from 'lucide-react';
import { AnimatedScore } from './components/AnimatedScore';
import { AnalyticsCard } from './components/AnalyticsCard';
import { Leaderboard } from './components/Leaderboard';
import { Podium } from './components/Podium';
import { useServerCountdown } from './hooks/useServerCountdown';
import type { Leader, Question, QuestionResult } from './types/quiz';
import './styles.css';

const API = import.meta.env.VITE_API_URL ?? 'http://localhost:3000';
let socket: Socket | undefined;
const connect = () => socket ??= io(API);

function Home() { return <main className="landing"><div className="brand-mark"><Zap size={18} fill="currentColor" /> TECHNICAL AWARENESS</div><div className="hero-grid"><section><p className="eyebrow">LIVE CLASSROOM QUIZ PLATFORM</p><h1>Make every answer<br /><em>count.</em></h1><p className="lede">A focused, fast-paced technical awareness quiz for your whole class. One room, forty questions, real-time results.</p><div className="home-actions"><a className="primary" href="/host"><MonitorPlay size={19} /> Host a quiz <ArrowRight size={17} /></a><a className="secondary" href="/join"><Users size={19} /> Join with a code</a></div></section><aside className="orbit-panel"><div className="orbit-ring"><Radio size={24} /><span>LIVE</span></div><div className="floating-stat stat-one"><strong>40</strong><span>questions</span></div><div className="floating-stat stat-two"><strong>15s</strong><span>per question</span></div><div className="floating-stat stat-three"><strong>1000</strong><span>max points</span></div></aside></div><footer><span>Built for real classrooms</span><span>•</span><span>Up to 40 players</span><span>•</span><span>Server-timed</span></footer></main> }


function Join() {
    const [codeValue, setCodeValue] = useState(''); const [name, setName] = useState(''); const [error, setError] = useState(''); const [joined, setJoined] = useState(false); const [state, setState] = useState<any>(); const [question, setQuestion] = useState<Question>(); const [result, setResult] = useState<QuestionResult>(); const [leaderboardOpen, setLeaderboardOpen] = useState(false); const [personalRank, setPersonalRank] = useState<any>(); const [progress, setProgress] = useState({ answered: 0, total: 0 }); const [fastest, setFastest] = useState<any>(); const [selected, setSelected] = useState(''); const time = useServerCountdown(question); const [leaders, setLeaders] = useState<Leader[]>([]); const [finished, setFinished] = useState(false);
    useEffect(() => { const s = connect(); s.on('question_started', (q: Question) => { setQuestion(q); setResult(undefined); setLeaderboardOpen(false); setFastest(undefined); setSelected(q.selectedKey ?? ''); }); s.on('question_result', setResult); s.on('answer_progress', setProgress); s.on('fastest_correct', setFastest); s.on('personal_rank', setPersonalRank); s.on('leaderboard_updated', setLeaders); s.on('leaderboard_opened', () => setLeaderboardOpen(true)); s.on('game_finished', (r: Leader[]) => { setLeaders(r); setFinished(true); }); return () => { s.off('question_started'); s.off('question_result'); s.off('answer_progress'); s.off('fastest_correct'); s.off('personal_rank'); s.off('leaderboard_updated'); s.off('leaderboard_opened'); s.off('game_finished'); }; }, []);
    useEffect(() => { if (!joined || !state?.gameCode) return; const s = connect(); const rejoin = () => s.emit('join_game', { gameCode: state.gameCode, name, reconnectToken: localStorage.getItem('quizReconnectToken') }, (reconnectResult: any) => { if (!reconnectResult?.error) setState(reconnectResult.state); }); s.on('connect', rejoin); return () => { s.off('connect', rejoin); }; }, [joined, state?.gameCode, name]);
    const join = () => { setError(''); connect().emit('join_game', { gameCode: codeValue, name, reconnectToken: localStorage.getItem('quizReconnectToken') }, (result: any) => { if (result.error) return setError(result.error); setState(result.state); localStorage.setItem('quizReconnectToken', result.reconnectToken); setJoined(true); }); };
    const answer = (key: string) => { if (selected || !question || time <= 0) return; setSelected(key); connect().emit('submit_answer', { questionId: question.id, selectedKey: key }, (result: any) => { if (result.error) setError(result.error); }); };
    if (finished) return <main className="app-shell"><header className="topbar"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a></header><section className="results-wrap"><p className="eyebrow">SESSION COMPLETE</p><h1>Nice work, {name || 'player'}.</h1><Podium leaders={leaders} /><Leaderboard leaders={leaders} /></section></main>;
    if (!joined) return <main className="app-shell centered"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a><section className="join-card"><p className="eyebrow">PLAYER ENTRY</p><h1>Step into the room.</h1><p className="muted">Enter the code your host shared with you.</p><label>GAME CODE<input value={codeValue} onChange={e => setCodeValue(e.target.value.toUpperCase())} placeholder="TECH42" maxLength={8} /></label><label>YOUR NAME<input value={name} onChange={e => setName(e.target.value)} placeholder="First name or nickname" maxLength={28} /></label>{error && <p className="error">{error}</p>}<button className="primary full" onClick={join}>Join quiz <ArrowRight size={17} /></button></section></main>;
    return <main className="app-shell"><header className="topbar"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a><span className="live-pill"><span /> {result ? (leaderboardOpen ? 'Leaderboard' : 'Answer reveal') : 'Waiting room'}</span></header><section className="player-view">{!question ? <div className="waiting"><div className="pulse-icon"><Radio /></div><p className="eyebrow">YOU'RE IN</p><h1>Eyes on the screen,<br />{name}.</h1><p className="muted">{state?.participants?.length ?? 1} players in the room. The host will begin shortly.</p></div> : result && leaderboardOpen ? <div className="question-result"><p className="eyebrow">LEADERBOARD</p><h1>Your position</h1><Leaderboard leaders={leaders} /><div className="result-detail"><span>YOUR RANK</span><strong>#{personalRank?.rank ?? leaders.find(leader => leader.name === name)?.rank ?? '-'}</strong></div></div> : result ? <div className="question-result"><p className="eyebrow">QUESTION {result.order} RESULT</p><h1>{result.isCorrect ? 'Correct!' : result.yourAnswer ? 'Not quite.' : 'Time is up.'}</h1><div className="result-detail"><span>Correct answer</span><strong>{result.correctAnswer ? `${result.correctAnswer.key}) ${result.correctAnswer.text}` : 'Unavailable'}</strong></div><div className="result-detail"><span>Your answer</span><strong>{result.yourAnswer ? `${result.yourAnswer.key}) ${result.yourAnswer.text}` : 'No answer'}</strong></div><div className="result-metrics"><div><span>TIME TAKEN</span><strong>{result.timeTaken === null ? '-' : `${result.timeTaken.toFixed(1)}s`}</strong></div><div><span>POINTS</span><strong>{result.score.toLocaleString()}</strong></div></div><p>{result.timeTaken === null ? 'No answer was submitted.' : `Your response time: ${result.timeTaken.toFixed(2)}s`}</p><p className={result.isCorrect ? 'submitted' : 'error'}>{result.isCorrect ? '✓ Your answer was correct.' : result.yourAnswer ? 'Your answer was incorrect.' : 'No answer was submitted.'}</p></div> : <div className="question-view"><div className="question-meta"><span>QUESTION {question.order} / 40</span><strong className={time < 5 ? 'urgent' : ''}>{time.toString().padStart(2, '0')}</strong></div><div className="timer-track"><span style={{ width: `${(time / question.timeLimit) * 100}%` }} /></div><h1>{question.text}</h1><div className="option-grid">{question.options.map(option => <button key={option.key} className={`option ${selected === option.key ? 'selected' : ''}`} onClick={() => answer(option.key)} disabled={Boolean(selected) || time === 0}><span>{option.key}</span>{option.text}</button>)}</div>{selected && <p className="submitted"><Check size={17} /> Answer submitted. Waiting for results...</p>}</div>}</section></main>;
}
function FinalAnalysis({ leaders, analytics, selectedParticipant, selectParticipant, sessionId }: { leaders: Leader[]; analytics: any; selectedParticipant: any; selectParticipant: (leader: Leader) => void; sessionId: string }) {
    return <main className="app-shell host-shell"><section className="host-content"><p className="eyebrow">QUIZ ANALYSIS</p><h1 className="finish-title">Final results</h1><Leaderboard leaders={leaders} onSelect={selectParticipant} /><AnalyticsCard analytics={analytics} />{selectedParticipant && <div className="participant-report"><h3>{selectedParticipant.participant.name}</h3><p>Rank {selectedParticipant.summary?.rank} · {selectedParticipant.summary?.correct} / 40 correct · {selectedParticipant.summary?.score.toLocaleString()} points</p>{selectedParticipant.answers.map((answer: any, index: number) => <div className="report-row" key={`${answer.questionOrder}-${index}`}><span>Q{index + 1}</span><strong>{answer.question}</strong><span>{answer.selectedAnswer ?? 'No answer'}</span><span>{answer.isCorrect ? 'Correct' : 'Incorrect'}</span><span>{answer.timeTaken === null ? '-' : `${answer.timeTaken.toFixed(1)}s`}</span><b>{answer.score}</b></div>)}</div>}<a className="secondary export" href={`${API}/api/sessions/${sessionId}/export`}>Export CSV</a></section></main>;
}

function Host() {
    const [qrDataUrl, setQrDataUrl] = useState(''); const [answerProgress, setAnswerProgress] = useState({ answered: 0, total: 0 }); const [analytics, setAnalytics] = useState<any>();
    const [session, setSession] = useState<any>(); const [leaders, setLeaders] = useState<Leader[]>([]); const [selectedParticipant, setSelectedParticipant] = useState<any>(); const [error, setError] = useState(''); const [question, setQuestion] = useState<Question>(); const [questionStats, setQuestionStats] = useState<any>(); const [leaderboardOpen, setLeaderboardOpen] = useState(false); const [copied, setCopied] = useState(false); const [ended, setEnded] = useState(false); const [showAnalytics, setShowAnalytics] = useState(false); const time = useServerCountdown(question);
    useEffect(() => { const s = connect(); s.on('question_started', (nextQuestion: Question) => { setQuestion(nextQuestion); setQuestionStats(undefined); setLeaderboardOpen(false); }); s.on('question_ended', setQuestionStats); s.on('leaderboard_updated', setLeaders); s.on('leaderboard_opened', () => setLeaderboardOpen(true)); s.on('participant_joined', (participant: any) => setSession((current: any) => current ? { ...current, participants: [...current.participants.filter((item: any) => item.id !== participant.id), participant] } : current)); s.on('game_finished', (r: Leader[]) => { setLeaders(r); setEnded(true); }); return () => { s.off('question_started'); s.off('question_ended'); s.off('leaderboard_updated'); s.off('leaderboard_opened'); s.off('participant_joined'); s.off('game_finished'); }; }, []);
    const create = async () => { try { const response = await fetch(`${API}/api/sessions`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ quizId: 'technical-awareness' }) }); const body = await response.text(); const data = body ? JSON.parse(body) : {}; if (!response.ok) return setError(data.error ?? 'Unable to create the live session.'); setSession(data); } catch { setError('Unable to reach the quiz server. Check that the backend is running.'); } };
    useEffect(() => { if (!session?.id) return; connect().emit('host_join_session', { sessionId: session.id }, (result: any) => { if (result?.error) return setError(result.error); if (result?.state) setSession(result.state); }); }, [session?.id]);
    useEffect(() => { if (!session?.gameCode) return; const joinUrl = `${window.location.origin}/join?code=${encodeURIComponent(session.gameCode)}`; QRCode.toDataURL(joinUrl, { width: 220, margin: 1, color: { dark: '#172015', light: '#d2e966' } }).then(setQrDataUrl).catch(() => setQrDataUrl('')); }, [session?.gameCode]);
    useEffect(() => { const s = connect(); const onProgress = (value: any) => setAnswerProgress(value); s.on('answer_progress', onProgress); return () => { s.off('answer_progress', onProgress); }; }, []);
    useEffect(() => { if (!ended || !session?.id) return; fetch(`${API}/api/sessions/${session.id}/analytics`).then(response => response.ok ? response.json() : null).then(setAnalytics).catch(() => setAnalytics(undefined)); }, [ended, session?.id]);
    if (!session) return <main className="app-shell centered"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a><section className="host-start"><div className="host-icon"><MonitorPlay /></div><p className="eyebrow">HOST CONSOLE</p><h1>Bring your room<br />to life.</h1><p className="muted">Create a live session from the 40-question Technical Awareness quiz.</p>{error && <p className="error">{error}</p>}<button className="primary full" onClick={create}>Create live session <ArrowRight size={17} /></button></section></main>;
    const start = () => connect().emit('host_start_game', { sessionId: session.id }, (r: any) => r?.error && setError(r.error)); const viewLeaderboard = () => connect().emit('host_view_leaderboard', { sessionId: session.id }, (r: any) => r?.error && setError(r.error)); const next = () => connect().emit('host_next_question', { sessionId: session.id }); const end = () => { if (window.confirm('End Quiz?\n\nAre you sure you want to end the quiz and show the final results?')) connect().emit('host_end_game', { sessionId: session.id }); }; const copy = () => { navigator.clipboard.writeText(session.gameCode); setCopied(true); setTimeout(() => setCopied(false), 1500); }; const selectParticipant = async (leader: Leader) => { const response = await fetch(`${API}/api/sessions/${session.id}/participants/${leader.id}/results`); if (response.ok) setSelectedParticipant(await response.json()); };
    if (ended && !showAnalytics) return <main className="app-shell host-shell"><section className="results-wrap"><p className="eyebrow">FINAL QUIZ RESULTS</p><h1>Podium</h1><Podium leaders={leaders} /><button className="primary full" onClick={() => setShowAnalytics(true)}>View quiz analysis <ArrowRight size={17} /></button></section></main>;
    if (ended && showAnalytics) return <FinalAnalysis leaders={leaders} analytics={analytics} selectedParticipant={selectedParticipant} selectParticipant={selectParticipant} sessionId={session.id} />;
    if (question && questionStats && leaderboardOpen) return <main className="app-shell host-shell"><header className="topbar"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a><span className="live-pill"><span /> Leaderboard</span></header><section className="host-content"><p className="eyebrow">QUESTION {questionStats.order} LEADERBOARD</p><h1 className="finish-title">Live leaderboard</h1><Leaderboard leaders={leaders} /><div className="host-actions"><button className="primary" onClick={next}>{question.order === 40 ? 'Finish quiz' : 'Next question'} <ArrowRight size={17} /></button><button className="danger" onClick={end}>End quiz</button></div></section></main>;
    if (question && questionStats) return <main className="app-shell host-shell"><header className="topbar"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a><span className="live-pill"><span /> Question result</span></header><section className="host-content"><p className="eyebrow">QUESTION {questionStats.order} COMPLETE</p><h1 className="finish-title">Answer reveal</h1><p className="correct-answer"><strong>Correct answer:</strong> {question.correctAnswer?.key}) {question.correctAnswer?.text}</p><div className="answer-stats"><span>Answered <strong>{questionStats.answered}</strong></span><span>Correct <strong>{questionStats.correct}</strong></span><span>Wrong <strong>{questionStats.wrong}</strong></span><span>No Answer <strong>{questionStats.unanswered}</strong></span></div><div className="distribution">{questionStats.distribution?.map((item: any) => <div className="distribution-row" key={item.key}><b>{item.key}</b><span><i style={{ width: `${questionStats.answered ? (item.count / questionStats.answered) * 100 : 0}%` }} /></span><strong>{item.count}</strong></div>)}</div><div className="host-actions"><button className="primary" onClick={viewLeaderboard}>View leaderboard <ArrowRight size={17} /></button><button className="danger" onClick={end}>End quiz</button></div></section></main>;
    return <main className="app-shell host-shell"><header className="topbar"><a className="brand" href="/"><Zap size={17} fill="currentColor" /> TECHNICAL AWARENESS</a><span className="live-pill"><span /> Host console</span></header><section className="host-content"><div className="host-header"><div><p className="eyebrow">LIVE SESSION</p><h1>{question ? `Question ${question.order}` : 'Lobby'}</h1></div><div className="code-block"><small>GAME CODE</small><strong>{session.gameCode}</strong><button onClick={copy} title="Copy game code">{copied ? <Check size={17} /> : <Copy size={17} />}</button></div></div>{ended ? <><p className="eyebrow">QUIZ COMPLETE</p><h2 className="finish-title">Final ranking</h2><Leaderboard leaders={leaders} onSelect={selectParticipant} />{selectedParticipant && <div className="participant-report"><h3>{selectedParticipant.participant.name}</h3><p>Rank {selectedParticipant.summary?.rank} · {selectedParticipant.summary?.correct} / 40 correct · {selectedParticipant.summary?.score.toLocaleString()} points</p>{selectedParticipant.answers.map((answer: any, index: number) => <div className="report-row" key={`${answer.questionOrder}-${index}`}><span>Q{index + 1}</span><strong>{answer.question}</strong><span>{answer.selectedAnswer ?? 'No answer'}</span><span>{answer.isCorrect ? 'Correct' : 'Incorrect'}</span><span>{answer.timeTaken === null ? '-' : `${answer.timeTaken.toFixed(1)}s`}</span><b>{answer.score}</b></div>)}</div>}<a className="secondary export" href={`${API}/api/sessions/${session.id}/export`}>Download CSV</a></> : question ? <div className="live-grid"><div className="live-question"><div className="question-meta"><span>QUESTION {question.order} / 40</span><strong className={time < 5 ? 'urgent' : ''}>{time.toString().padStart(2, '0')}</strong></div><div className="timer-track"><span style={{ width: `${(time / question.timeLimit) * 100}%` }} /></div><h2>{question.text}</h2><div className="host-options">{question.options.map(o => <div key={o.key}><b>{o.key}</b>{o.text}</div>)}</div>{question.correctAnswer && <p className="correct-answer"><strong>Correct Answer:</strong> {question.correctAnswer.key}) {question.correctAnswer.text}</p>}{questionStats && <div className="answer-stats"><span>Answered <strong>{questionStats.answered}</strong></span><span>Correct <strong>{questionStats.correct}</strong></span><span>Wrong <strong>{questionStats.wrong}</strong></span><span>No Answer <strong>{questionStats.unanswered}</strong></span></div>}<div className="host-actions"><button className="primary" onClick={next}>{question.order === 40 ? 'Finish quiz' : 'Next question'} <ArrowRight size={17} /></button><button className="danger" onClick={end}>End quiz</button></div></div><aside className="side-panel"><div className="panel-title"><span>LIVE LEADERBOARD</span><Users size={17} /></div><Leaderboard leaders={leaders} /></aside></div> : <div className="lobby"><div className="lobby-code"><span>Students join at</span><strong>/join</strong><p>Share the game code above with your class.</p></div><div className="waiting-list"><div className="panel-title"><span>CONNECTED STUDENTS</span><strong>{session.participants.length} / 40</strong></div>{session.participants.length ? session.participants.map((p: any, i: number) => <div className="student" key={p.id}><span>{String(i + 1).padStart(2, '0')}</span>{p.name}<i /></div>) : <p className="muted">Students will appear here as they join.</p>}<button className="primary full" onClick={start} disabled={!session.participants.length}>Start quiz <Radio size={17} /></button></div></div>}</section></main>;
}
function App() { const path = window.location.pathname; return path.startsWith('/host') ? <Host /> : path.startsWith('/join') ? <Join /> : <Home />; }

createRoot(document.getElementById('root')!).render(<React.StrictMode><App /></React.StrictMode>);
